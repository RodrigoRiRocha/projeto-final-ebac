{
  "buildCommand": "uv run --with-requirements requirements.txt python manage.py migrate --noinput && uv run --with-requirements requirements.txt python manage.py collectstatic --noinput",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}